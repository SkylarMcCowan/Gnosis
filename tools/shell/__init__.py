"""shell.* tools: running commands (tests, code execution, git). Not
implemented yet - see TODO.md Phase 2 ("Shell execution becomes a tool",
"Code execution becomes a tool", "Git operations become tools", "Testing
becomes a tool"). Should default to Permission.REQUIRES_APPROVAL or
stricter once Phase 15's governance actually enforces the `permission`
attribute tools already carry - nothing in this category should default to
SAFE.
"""
