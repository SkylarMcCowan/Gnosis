"""Alignment & Deity Compliance ("Falling From Grace") - design in
docs/gnosis_crawler_alignment.md. Pure rules-engine code: the compliance
evaluator here never calls an LLM and never asks one anything (Section
2.1's "AI never overrides mechanics") - it only emits events a narrator
can later describe.
"""
